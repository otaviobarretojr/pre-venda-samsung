(()=>{
'use strict';
const RELEASE='8.6.5';
const state={online:navigator.onLine,syncing:false,lastSync:null,lastError:null,mode:'event-driven'};
function pill(){let el=document.getElementById('v711SyncStatus');if(el)return el;el=document.createElement('button');el.id='v711SyncStatus';el.type='button';el.style.cssText='position:fixed;right:14px;bottom:14px;z-index:9999;border:1px solid #d1d5db;border-radius:999px;background:#fff;padding:8px 12px;font:600 12px system-ui;box-shadow:0 6px 24px rgba(0,0,0,.12);cursor:pointer';el.onclick=()=>refresh(true);document.body?.appendChild(el);return el}
function paint(){const el=pill();if(!el)return;let text;if(!state.online)text='Offline • dados locais';else if(state.syncing)text='Sincronizando…';else if(state.lastError)text='Sync pendente • toque para tentar';else if(state.lastSync)text='Online • '+new Date(state.lastSync).toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'});else text='Online';el.textContent=text;el.title='V'+RELEASE+' • sincronização por ação; toque para sincronizar manualmente';document.documentElement.dataset.syncState=!state.online?'offline':state.syncing?'syncing':state.lastError?'error':'online'}
async function refresh(manual=false){if(!navigator.onLine){state.online=false;paint();return false}if(state.syncing)return false;state.online=true;state.syncing=true;state.lastError=null;paint();try{const d=window.V71Data;if(!d)throw Error('Camada de dados indisponível');await Promise.allSettled([d.preSale.list(),d.budgetCatalog.list(),d.ecosystem.list()]);state.lastSync=Date.now();window.dispatchEvent(new CustomEvent('pre-sale:v711-synced',{detail:{at:state.lastSync,manual}}));return true}catch(e){state.lastError=e.message||String(e);console.warn('[V8.6.5] sync',e);return false}finally{state.syncing=false;paint()}}
function markAction(detail={}){state.lastSync=Date.now();state.lastError=null;paint();window.dispatchEvent(new CustomEvent('pre-sale:v865-action-synced',{detail:{...detail,at:state.lastSync}}))}
window.addEventListener('online',()=>{state.online=true;paint()});window.addEventListener('offline',()=>{state.online=false;paint()});window.addEventListener('presale:delivered',e=>markAction({type:'delivered',...(e.detail||{})}));window.addEventListener('pre-sale:v865-mutated',e=>markAction(e.detail||{}));
window.V711_STABILITY={state,refresh,markAction,release:RELEASE};paint();setTimeout(()=>refresh(false),900);
})();