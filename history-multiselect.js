(()=>{
'use strict';
const $=id=>document.getElementById(id);
let selected=new Set();
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function products(){return [...new Set(getHistory().map(x=>x.produto).filter(Boolean))].sort((a,b)=>a.localeCompare(b,'pt-BR'))}
function ensure(){const old=$('histProduct');if(!old||$('histProductMulti'))return;old.style.display='none';const box=document.createElement('div');box.id='histProductMulti';box.className='hist-product-multi';box.innerHTML='<button type="button" id="histProductMultiBtn" class="hist-multi-btn">Todos os produtos</button><div id="histProductMultiMenu" class="hist-multi-menu" hidden><div class="hist-multi-head"><strong>Produtos</strong><button type="button" id="histProductClear">Limpar</button></div><div id="histProductChecks"></div></div>';old.insertAdjacentElement('afterend',box);$('histProductMultiBtn').onclick=e=>{e.stopPropagation();$('histProductMultiMenu').hidden=!$('histProductMultiMenu').hidden};$('histProductClear').onclick=()=>{selected.clear();render();renderHistory()};document.addEventListener('click',e=>{if(!box.contains(e.target))$('histProductMultiMenu').hidden=true});render()}
function render(){ensure();const wrap=$('histProductChecks');if(!wrap)return;const list=products();for(const x of [...selected])if(!list.includes(x))selected.delete(x);wrap.innerHTML=list.map((p,i)=>`<label class="hist-multi-option"><input type="checkbox" data-product="${esc(p)}" ${selected.has(p)?'checked':''}><span>${esc(p)}</span></label>`).join('')||'<div class="hist-multi-empty">Nenhum produto no histórico.</div>';wrap.querySelectorAll('input').forEach(i=>i.onchange=()=>{i.checked?selected.add(i.dataset.product):selected.delete(i.dataset.product);updateLabel();renderHistory()});updateLabel()}
function updateLabel(){const b=$('histProductMultiBtn');if(!b)return;b.textContent=!selected.size?'Todos os produtos':selected.size===1?[...selected][0]:`${selected.size} produtos selecionados`}
const r0=window.renderHistory;window.renderHistory=function(){ensure();const old=$('histProduct');if(old)old.value='';r0();render();if(!selected.size)return;const tbody=$('historyContent')?.querySelector('tbody');if(!tbody)return;[...tbody.rows].forEach(row=>{const product=(row.cells[2]?.textContent||'').split('•')[0].trim();row.style.display=selected.has(product)?'':'none'});const visible=[...tbody.rows].filter(r=>r.style.display!=='none').length;const meta=$('historyContent')?.querySelector('.history-meta');if(meta)meta.innerHTML=`<strong>${visible}</strong> resultado(s) nesta página • ${selected.size} produto(s) selecionado(s)`};
const style=document.createElement('style');style.textContent='.hist-product-multi{position:relative;min-width:0}.hist-multi-btn{width:100%;min-height:42px;padding:10px 11px;border:1px solid #d9dfeb;border-radius:11px;background:#fff;color:#344054;text-align:left;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.hist-multi-menu{position:absolute;z-index:80;top:46px;left:0;min-width:280px;max-width:min(360px,90vw);max-height:330px;overflow:auto;background:#fff;border:1px solid #d0d5dd;border-radius:12px;box-shadow:0 12px 30px rgba(16,24,40,.16);padding:8px}.hist-multi-head{display:flex;align-items:center;justify-content:space-between;padding:6px 7px 9px;border-bottom:1px solid #eaecf0}.hist-multi-head button{border:0;background:transparent;color:#1428A0;font-weight:800}.hist-multi-option{display:flex;gap:9px;align-items:flex-start;padding:9px 7px;border-radius:8px;cursor:pointer;font-size:12px}.hist-multi-option:hover{background:#f5f7fb}.hist-multi-option input{margin-top:2px}.hist-multi-empty{padding:12px;color:#667085;font-size:12px}@media(max-width:760px){.hist-product-multi{grid-column:1/-1}.hist-multi-menu{position:fixed;left:12px;right:12px;top:auto;bottom:78px;max-width:none;max-height:55vh}}';document.head.appendChild(style);ensure();render();
})();